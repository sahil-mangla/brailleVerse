# 🏗️ ARCHITECTURE OVERVIEW

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                    (Browser - Chrome/Edge/Safari)               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Requests
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DJANGO WEB SERVER                          │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    braille_project/                     │  │
│  │                                                         │  │
│  │  ├── settings.py    (Configuration)                    │  │
│  │  ├── urls.py        (Main Routing)                     │  │
│  │  └── wsgi.py        (WSGI Server)                      │  │
│  └─────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                      braille_app/                       │  │
│  │                                                         │  │
│  │  ├── views.py       (Business Logic)                   │  │
│  │  ├── urls.py        (App Routing)                      │  │
│  │  ├── models.py      (Database Models)                  │  │
│  │  └── firebase_service.py (Firebase Integration)        │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   ▼
        ┌─────────────────┐  ┌─────────────────────┐
        │   TEMPLATES     │  │  FIREBASE SERVICE   │
        │                 │  │                     │
        │  ├── base.html  │  │  Text Chunking     │
        │  ├── landing    │  │  Sequential Send   │
        │  ├── main_menu  │  │  Mock Mode         │
        │  └── ...        │  │                     │
        └─────────────────┘  └─────────────────────┘
                │                       │
                │                       │
                ▼                       ▼
        ┌─────────────────┐  ┌─────────────────────┐
        │  STATIC FILES   │  │  FIREBASE REALTIME  │
        │                 │  │     DATABASE        │
        │  ├── voice.js   │  │                     │
        │  └── style.css  │  │  /braille_display/  │
        └─────────────────┘  │       /text         │
                              └─────────────────────┘
                                       │
                                       │ Real-time Sync
                                       ▼
                              ┌─────────────────────┐
                              │   ESP32 HARDWARE    │
                              │                     │
                              │  Braille Display    │
                              │  Conversion Logic   │
                              └─────────────────────┘
```

---

## Data Flow Diagram

### Flow 1: User Sends Custom Text

```
┌──────────┐
│  USER    │
│ (Speaks) │
└────┬─────┘
     │ Voice Command
     ▼
┌──────────────────┐
│  voice.js        │ ◄── Web Speech API (SpeechRecognition)
│  (Browser)       │
└────┬─────────────┘
     │ Transcribed Text
     ▼
┌──────────────────┐
│  Django View     │
│  custom_text()   │
└────┬─────────────┘
     │ Text String
     ▼
┌──────────────────────────────┐
│  firebase_service.py         │
│  ├── chunk_text()            │ ◄── Splits into 80-char chunks
│  └── send_text_to_device()   │
└────┬─────────────────────────┘
     │ Chunked Text
     ▼
┌──────────────────┐
│  Firebase        │
│  Realtime DB     │
│                  │
│  {               │
│    text: "...",  │
│    chunk: 1/5,   │
│    timestamp     │
│  }               │
└────┬─────────────┘
     │ Real-time Sync
     ▼
┌──────────────────┐
│  ESP32 Device    │
│  (Listening)     │
└────┬─────────────┘
     │ Convert to Braille
     ▼
┌──────────────────┐
│  Braille Display │
│  (Physical)      │
└──────────────────┘
```

### Flow 2: Voice Navigation

```
┌──────────┐
│  USER    │
│ (Speaks) │
└────┬─────┘
     │ "Main Menu"
     ▼
┌──────────────────┐
│  voice.js        │
│  VoiceNavigator  │
│  .recognize()    │
└────┬─────────────┘
     │ Command String
     ▼
┌──────────────────┐
│  processCommand()│ ◄── Parses command
└────┬─────────────┘
     │
     ├──────► "Main Menu" ──► Navigate to /main-menu/
     ├──────► "Back"      ──► window.history.back()
     ├──────► "Help"      ──► speak(helpText)
     └──────► "One"       ──► Click button[0]
     
     ▼
┌──────────────────┐
│  Django View     │
│  main_menu()     │
└────┬─────────────┘
     │ Context Data
     ▼
┌──────────────────┐
│  Template        │
│  main_menu.html  │
└────┬─────────────┘
     │ HTML + Voice Triggers
     ▼
┌──────────────────┐
│  Browser         │
│  (Render & Speak)│
└──────────────────┘
```

---

## Component Breakdown

### 1. Frontend Layer (Browser)

```
┌─────────────────────────────────────┐
│         HTML Templates              │
│  ┌──────────────────────────────┐  │
│  │  base.html                   │  │
│  │  ├── Header                  │  │
│  │  ├── Main Content (block)    │  │
│  │  ├── Voice Controls          │  │
│  │  └── Scripts                 │  │
│  └──────────────────────────────┘  │
│                                     │
│         JavaScript                  │
│  ┌──────────────────────────────┐  │
│  │  voice.js                    │  │
│  │  ├── VoiceNavigator class    │  │
│  │  ├── SpeechRecognition       │  │
│  │  ├── SpeechSynthesis         │  │
│  │  └── Command Processing      │  │
│  └──────────────────────────────┘  │
│                                     │
│         CSS                         │
│  ┌──────────────────────────────┐  │
│  │  style.css                   │  │
│  │  ├── High Contrast Theme     │  │
│  │  ├── Large Fonts             │  │
│  │  ├── Focus Indicators        │  │
│  │  └── Responsive Layout       │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

### 2. Backend Layer (Django)

```
┌─────────────────────────────────────┐
│         Django Project              │
│  ┌──────────────────────────────┐  │
│  │  braille_project/            │  │
│  │  ├── settings.py             │  │
│  │  │   ├── INSTALLED_APPS      │  │
│  │  │   ├── FIREBASE_CONFIG     │  │
│  │  │   └── DEVICE_CHAR_LIMIT   │  │
│  │  ├── urls.py (Root Routing)  │  │
│  │  └── wsgi.py (Server)        │  │
│  └──────────────────────────────┘  │
│                                     │
│         Django App                  │
│  ┌──────────────────────────────┐  │
│  │  braille_app/                │  │
│  │  ├── views.py                │  │
│  │  │   ├── landing()           │  │
│  │  │   ├── main_menu()         │  │
│  │  │   ├── custom_text()       │  │
│  │  │   ├── voice_assistant()   │  │
│  │  │   ├── news()              │  │
│  │  │   └── books()             │  │
│  │  ├── urls.py (App Routing)   │  │
│  │  └── firebase_service.py     │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

### 3. Firebase Integration Layer

```
┌─────────────────────────────────────┐
│    firebase_service.py              │
│                                     │
│  ┌──────────────────────────────┐  │
│  │  FirebaseService class       │  │
│  │                              │  │
│  │  ├── initialize()            │  │
│  │  │   └── Load credentials    │  │
│  │  │                            │  │
│  │  ├── chunk_text()            │  │
│  │  │   ├── Split by words      │  │
│  │  │   └── Respect boundaries  │  │
│  │  │                            │  │
│  │  ├── send_text_to_device()   │  │
│  │  │   ├── Chunk text          │  │
│  │  │   ├── Send sequentially   │  │
│  │  │   └── Add delays          │  │
│  │  │                            │  │
│  │  └── send_single_message()   │  │
│  │      └── Quick notifications │  │
│  └──────────────────────────────┘  │
│                                     │
│         Mock Mode                   │
│  ┌──────────────────────────────┐  │
│  │  If no credentials:          │  │
│  │  ├── Log to console          │  │
│  │  ├── Simulate sending        │  │
│  │  └── Full functionality      │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## Page Navigation Flow

```
                    ┌──────────────┐
                    │   LANDING    │
                    │              │
                    │ "Are you VI  │
                    │ or Helper?"  │
                    └──────┬───────┘
                           │
              ┌────────────┴─────────────┐
              │                          │
              ▼                          ▼
    ┌─────────────────┐        ┌─────────────────┐
    │  Visually       │        │    Helper       │
    │  Impaired       │        │                 │
    └────────┬────────┘        └────────┬────────┘
             │                          │
             ▼                          │
    ┌─────────────────┐                │
    │   USER TYPE     │                │
    │  Fully/Partial  │                │
    └────────┬────────┘                │
             │                          │
             └────────────┬─────────────┘
                          │
                          ▼
                ┌─────────────────┐
                │   MAIN MENU     │
                │                 │
                │  1. Custom Text │
                │  2. Assistant   │
                │  3. News        │
                │  4. Books       │
                │  5. Back        │
                └────────┬────────┘
                         │
        ┌────────────────┼────────────────┬─────────────┐
        │                │                │             │
        ▼                ▼                ▼             ▼
┌──────────────┐  ┌──────────────┐  ┌─────────┐  ┌─────────┐
│ CUSTOM TEXT  │  │ VOICE ASSIST │  │  NEWS   │  │  BOOKS  │
│              │  │              │  │         │  │         │
│ Input Text   │  │ Ask Question │  │ Browse  │  │ Library │
│ Send to FB   │  │ Get Answer   │  │ Articles│  │ Read    │
└──────────────┘  └──────────────┘  └────┬────┘  └────┬────┘
                                          │            │
                                          ▼            ▼
                                    ┌─────────┐  ┌─────────┐
                                    │Category │  │ Chapter │
                                    │Articles │  │ Reader  │
                                    └─────────┘  └─────────┘
```

---

## Voice Command Processing

```
User Says: "Send Custom Text"
         │
         ▼
┌─────────────────────────┐
│ SpeechRecognition API   │
│ (Browser)               │
└────────┬────────────────┘
         │ Transcript
         ▼
┌─────────────────────────┐
│ VoiceNavigator.js       │
│ processVoiceCommand()   │
└────────┬────────────────┘
         │
         ▼ Parse Command
         │
    ┌────┴─────────────────────────────┐
    │                                  │
    ▼ Option Name?               ▼ Number?
┌─────────┐                  ┌──────────┐
│ "Custom │                  │  "One"   │
│  Text"  │                  │  "Two"   │
└────┬────┘                  └────┬─────┘
     │                            │
     └──────────┬─────────────────┘
                │
                ▼
        Find matching button
                │
                ▼
┌─────────────────────────────┐
│ Speak: "Selecting Custom    │
│ Text"                       │
└────────┬────────────────────┘
         │
         ▼
    Click button / Navigate
         │
         ▼
┌─────────────────────────────┐
│ Load /custom-text/ page     │
└─────────────────────────────┘
```

---

## Firebase Data Structure

```
Firebase Realtime Database:

/
└── braille_display/
    └── text/
        ├── text: "Hello, this is the first chunk of..."
        ├── chunk_number: 1
        ├── total_chunks: 5
        ├── timestamp: 1703174400
        └── type: "content"

ESP32 Listens to: /braille_display/text

On Change:
1. Read text field
2. Convert to braille (device logic)
3. Display on pins
4. Wait for next chunk
```

---

## Accessibility Architecture

```
┌─────────────────────────────────────────┐
│         ACCESSIBILITY LAYERS            │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Layer 1: VISUAL                  │ │
│  │  ├── High Contrast Colors         │ │
│  │  ├── Large Fonts (1.25rem+)       │ │
│  │  ├── Clear Focus Indicators       │ │
│  │  └── Minimal Clutter              │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Layer 2: VOICE OUTPUT            │ │
│  │  ├── SpeechSynthesis              │ │
│  │  ├── Auto-narration               │ │
│  │  ├── Command feedback             │ │
│  │  └── Error announcements          │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Layer 3: VOICE INPUT             │ │
│  │  ├── SpeechRecognition            │ │
│  │  ├── Command processing           │ │
│  │  └── Voice-to-text                │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Layer 4: SCREEN READERS          │ │
│  │  ├── ARIA labels                  │ │
│  │  ├── Semantic HTML                │ │
│  │  ├── Live regions                 │ │
│  │  └── Role attributes              │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Layer 5: KEYBOARD                │ │
│  │  ├── Tab navigation               │ │
│  │  ├── Shortcuts (Ctrl+Space, H, R) │ │
│  │  ├── Enter/Space activation       │ │
│  │  └── Escape to stop               │ │
│  └───────────────────────────────────┘ │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Layer 6: BRAILLE OUTPUT          │ │
│  │  ├── Firebase integration         │ │
│  │  ├── Text chunking                │ │
│  │  └── Physical braille device      │ │
│  └───────────────────────────────────┘ │
└─────────────────────────────────────────┘

Each layer works independently!
User can use any combination.
```

---

## Technology Stack

```
┌─────────────────────────────────────────┐
│            FRONTEND                     │
│  ┌──────────────────────────────────┐  │
│  │  HTML5 (Semantic)                │  │
│  │  CSS3 (Accessibility-first)      │  │
│  │  JavaScript (Vanilla, ES6+)      │  │
│  │  Web Speech API                  │  │
│  │    ├── SpeechRecognition         │  │
│  │    └── SpeechSynthesis           │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│            BACKEND                      │
│  ┌──────────────────────────────────┐  │
│  │  Python 3.8+                     │  │
│  │  Django 5.0                      │  │
│  │  Django Templates                │  │
│  │  SQLite (Database)               │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│         INTEGRATION                     │
│  ┌──────────────────────────────────┐  │
│  │  Firebase Admin SDK              │  │
│  │  Firebase Realtime Database      │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│           HARDWARE                      │
│  ┌──────────────────────────────────┐  │
│  │  ESP32 Microcontroller           │  │
│  │  Refreshable Braille Display     │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## Development vs Production

```
DEVELOPMENT MODE:
┌──────────────────────────┐
│ Django Dev Server        │
│ ├── DEBUG = True         │
│ ├── Mock Firebase        │
│ ├── Console logging      │
│ └── localhost:8000       │
└──────────────────────────┘

PRODUCTION MODE:
┌──────────────────────────┐
│ Gunicorn + Nginx         │
│ ├── DEBUG = False        │
│ ├── Real Firebase        │
│ ├── Error tracking       │
│ └── HTTPS domain         │
└──────────────────────────┘
```

---

**This architecture provides:**
- ✅ Separation of concerns
- ✅ Scalability
- ✅ Maintainability
- ✅ Testability
- ✅ Accessibility at every layer
- ✅ Works offline (without Firebase)
- ✅ Easy to extend
