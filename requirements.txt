# Django and Firebase Dependencies for Braille Display Website

# Core Django
Django>=5.0.0

# Firebase Realtime Database (lightweight REST client, not the heavy Admin SDK)
requests>=2.31.0

# News API for real-time news
newsapi-python>=0.2.7

# Google Gemini AI (pinned to 1.x for the new Client API)
google-genai>=1.0.0

# PDF text extraction
PyPDF2>=3.0.0

# Image processing
Pillow>=10.0.0

# Production deployment
gunicorn>=21.2.0
whitenoise>=6.6.0
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0
